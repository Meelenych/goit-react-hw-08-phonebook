import React from "react";
import styles from "../views/Views.module.css";

const HomeView = () => (
  <div className={styles.homeView}>
    <h1 className={styles.titleHome}>Not all those who wander are lost...</h1>
  </div>
);

export default HomeView;
